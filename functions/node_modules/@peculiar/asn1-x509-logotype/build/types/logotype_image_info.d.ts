import { LogotypeImageResolution } from "./logotype_image_resolution";
/**
 * ```asn1
 * LogotypeImageType ::= INTEGER { grayScale(0), color(1) }
 * ```
 */
export declare enum LogotypeImageType {
    grayScale = 0,
    color = 1
}
/**
 * ```asn1
 * LogotypeImageInfo ::= SEQUENCE {
 *   type            [0] LogotypeImageType DEFAULT color,
 *   fileSize        INTEGER,  -- In octets
 *   xSize           INTEGER,  -- Horizontal size in pixels
 *   ySize           INTEGER,  -- Vertical size in pixels
 *   resolution      LogotypeImageResolution OPTIONAL,
 *   language        [4] IA5String OPTIONAL }  -- RFC 3066 Language Tag
 * ```
 */
export declare class LogotypeImageInfo {
    type: LogotypeImageType;
    /**
     * In octets
     */
    fileSize: number;
    /**
     * Horizontal size in pixels
     */
    xSize: number;
    /**
     * Vertical size in pixels
     */
    ySize: number;
    resolution?: LogotypeImageResolution;
    /**
     * RFC 3066 Language Tag
     */
    language?: string;
    constructor(params?: Partial<LogotypeImageInfo>);
}
